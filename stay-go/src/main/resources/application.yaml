spring:
  application:
    name: stay-go
  datasource:
    url: jdbc:mysql://localhost:3306/stay_go
    username: root
    password: werpipl15
  jpa:
    hibernate:
      ddl-auto: update
logging:
  level:
    org:
      hibernate: info
      springframework:
        jdbc: info
    root: info
  file:
    name: createTable.log