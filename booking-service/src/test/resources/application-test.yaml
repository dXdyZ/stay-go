spring:
  flyway:
    enabled: true
    locations: classpath:db/test-migration
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate.generate_statistics: true
logging:
  level:
    org.hibernate.stat: debug